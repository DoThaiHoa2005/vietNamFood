<Window x:Class="VietnamFoodGuide.Views.AudioGeneratorLauncher"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🎵 Tạo File Audio" Height="200" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="🎵 Tạo File Audio MP3" 
                   FontSize="20" FontWeight="Bold"
                   Foreground="#1D3557"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Description -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Tạo file audio MP3 cho tất cả quán ăn" 
                       FontSize="14" TextWrapping="Wrap"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>
            <TextBlock Text="✅ 3 ngôn ngữ: Tiếng Việt, English, 中文" 
                       FontSize="12" Foreground="#555"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,5"/>
            <TextBlock Text="✅ Lưu vào: Assets/Audio/" 
                       FontSize="12" Foreground="#555"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,5"/>
            <TextBlock Text="⏱️ Thời gian: ~2-5 phút" 
                       FontSize="12" Foreground="#555"
                       HorizontalAlignment="Center"/>
        </StackPanel>

        <!-- Button -->
        <Button Grid.Row="2" Content="🚀 Bắt Đầu Tạo Audio" 
                FontSize="14" FontWeight="Bold"
                Background="#6C5CE7" Foreground="White"
                Padding="20,12" Cursor="Hand"
                Click="StartGeneration_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="8"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#5F4FD1"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#5243C0"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
