<Window x:Class="VietnamFoodGuide.Views.MapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Vietnam Food Guide - Map"
        Height="800" Width="1100" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}"
        Icon="/Assets/Images/banner.png"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style TargetType="Button" x:Key="NavButtonStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="White" CornerRadius="12" Padding="15,10" Cursor="Hand">
                            <Border.Effect>
                                <DropShadowEffect BlurRadius="10" Opacity="0.1" ShadowDepth="3"/>
                            </Border.Effect>
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0E0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header Info -->
        <Border Grid.Row="0" Background="White" CornerRadius="15" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" Opacity="0.08" ShadowDepth="4"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Back Button -->
                <Button Grid.Column="0" Style="{StaticResource NavButtonStyle}" Click="Back_Click"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⬅" FontSize="18" Margin="0,0,10,0"/>
                        <TextBlock Text="Quay lại" FontSize="14" FontWeight="Medium" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <!-- Food Info -->
                <StackPanel Grid.Column="1" Margin="30,0,0,0" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtName" FontSize="26" FontWeight="ExtraBold"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                    <TextBlock x:Name="TxtCity" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,6,0,0"/>
                </StackPanel>

                <!-- Rating -->
                <StackPanel Grid.Column="2" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <ItemsControl x:Name="StarRating" Focusable="False">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="★" Foreground="{StaticResource AccentBrush}" FontSize="18" Margin="0,0,3,0"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                    <TextBlock x:Name="TxtRatingCount" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Map Container -->
        <Border Grid.Row="1" CornerRadius="20" ClipToBounds="True" Background="White">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.12" ShadowDepth="8"/>
            </Border.Effect>
            <Grid>
                <wv2:WebView2 x:Name="MapBrowser"/>
                <!-- Auto-narration overlay -->
                <Border x:Name="NarrationBanner" VerticalAlignment="Bottom" HorizontalAlignment="Stretch"
                        Background="#CC000000" CornerRadius="0,0,20,20" Padding="20,12"
                        Visibility="Collapsed">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="🔊 Đang thuyết minh tự động" Foreground="White" FontSize="12" Opacity="0.8"/>
                            <TextBlock x:Name="TxtNarrationName" Foreground="White" FontSize="16" FontWeight="Bold" TextWrapping="Wrap"/>
                        </StackPanel>
                        <Button Grid.Column="1" x:Name="BtnStopNarration" Content="⏹ Dừng"
                                Background="#E63946" Foreground="White" Padding="12,8"
                                BorderThickness="0" Cursor="Hand" Click="StopNarration_Click"
                                VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </Grid>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>