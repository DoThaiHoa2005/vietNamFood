<Window x:Class="VietnamFoodGuide.Views.MapWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Vietnam Food Guide - Map" 
        Height="750" Width="1000" WindowStartupLocation="CenterScreen"
        Background="#F8F9FA">

    <Window.Resources>
        <Style TargetType="Button" x:Key="NavButtonStyle">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="Transparent" CornerRadius="8" Padding="12,6">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#EFEFEF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0" HorizontalAlignment="Left" Style="{StaticResource NavButtonStyle}" 
                Click="Back_Click" Margin="0,0,0,66">
            <StackPanel Orientation="Horizontal" Height="146" VerticalAlignment="Top">
                <TextBlock Text="&#xE72B;" FontFamily="Segoe MDL2 Assets" FontSize="16" Foreground="#E63946" VerticalAlignment="Center"/>
                <TextBlock Text="Quay lại" FontSize="15" FontWeight="Medium" Margin="8,0,0,0" Foreground="#4A4A4A" Height="26"/>
            </StackPanel>
        </Button>
        <StackPanel VerticalAlignment="Top" Margin="140,21,-15,0" Height="137">
            <TextBlock x:Name="TxtName" FontSize="24" FontWeight="ExtraBold" Foreground="#1A1A1A"/>
            <TextBlock x:Name="TxtCity" FontSize="14" Foreground="#888" Margin="0,4,0,0"/>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <ItemsControl x:Name="StarRating" Focusable="False">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="★" Foreground="#FDCB6E" FontSize="16" Margin="0,0,2,0"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                <TextBlock x:Name="TxtRatingCount" FontSize="12" Foreground="#636E72" Margin="8,2,0,0" VerticalAlignment="Center"/>
            </StackPanel>

            <Border Background="#E8F4FF" CornerRadius="8" HorizontalAlignment="Left" Padding="8,4" Margin="0,15,0,0" Height="32">
                <TextBlock Text="Vị trí món ăn" FontSize="10" Foreground="#0984E3" FontWeight="SemiBold" RenderTransformOrigin="0.458,-2.255"/>
            </Border>
        </StackPanel>

        <Border Grid.Row="2" CornerRadius="24" ClipToBounds="True" Background="#EFEFEF">
            <wv2:WebView2 x:Name="MapBrowser" Margin="-20,-73,-10,0" />
        </Border>
    </Grid>
</Window>